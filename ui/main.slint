// Generated by Unloop - 2026-01-01T21:09:10.976Z
// Smart Air Fryer: Elegant touch control for air frying with presets and manual control

import { MaterialWindow } from "@material";
import { AppData } from "data.slint";
import { HomeScreen } from "screens.slint";
import { PresetsScreen } from "screens.slint";
import { ControlScreen } from "screens.slint";
import { SettingsScreen } from "screens.slint";

export component App inherits MaterialWindow {
    title: "Smart Air Fryer";
    width: 800px;
    height: 480px;
    background: #1E1E1E;
    
    in-out property <string> active-screen: "home";
    in-out property <AppData> data: {
        current-temp: 75,
        target-temp: 375,
        time-remaining: 0,
        cook-progress: 0,
        cooking-status: "ready",
        basket-status: "ok",
        cooking-presets: "[]",
        temp-unit: "°F",
        auto-preheat: true,
        shake-reminder: true,
        screen-brightness: 80,
        sound-enabled: true,
        last-cleaned: "Never",
        total-usage-hours: 0,

    };
    
    callback start-pause-clicked();
    callback stop-clicked();
    callback target-changed(float);
    callback mode-changed(string);
    
    VerticalLayout {
        Rectangle {
            vertical-stretch: 1;
            
            if active-screen == "home" : HomeScreen {
                data <=> root.data;
            }

            if active-screen == "presets" : PresetsScreen {
                data <=> root.data;
            }

            if active-screen == "control" : ControlScreen {
                data <=> root.data;
            }

            if active-screen == "settings" : SettingsScreen {
                data <=> root.data;
            }

        }
        
        Rectangle {
            height: 80px;
            background: #2A2A2A;
            
            HorizontalLayout {
                alignment: space-evenly;
                
                Rectangle {
                    horizontal-stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "home"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Home";
                            font-size: 12px;
                            color: active-screen == "home" ? #3AA1F5 : #9E9E9E;
                            horizontal-alignment: center;
                        }
                    }
                }
                Rectangle {
                    horizontal-stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "presets"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Presets";
                            font-size: 12px;
                            color: active-screen == "presets" ? #3AA1F5 : #9E9E9E;
                            horizontal-alignment: center;
                        }
                    }
                }
                Rectangle {
                    horizontal-stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "control"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Manual";
                            font-size: 12px;
                            color: active-screen == "control" ? #3AA1F5 : #9E9E9E;
                            horizontal-alignment: center;
                        }
                    }
                }
                Rectangle {
                    horizontal-stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "settings"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Settings";
                            font-size: 12px;
                            color: active-screen == "settings" ? #3AA1F5 : #9E9E9E;
                            horizontal-alignment: center;
                        }
                    }
                }
            }
        }
    }
}
